#!/bin/sh

set -o errexit
set -o nounset
set -o pipefail

# Run Hugo development server publishing drafts to a custom location to
# facilitate working on new content.
ROOT_DIR="$(dirname "$0")/.."
DEV_DIR="$ROOT_DIR/dev"
rm -rf "$DEV_DIR"
exec hugo server -wDs "$ROOT_DIR" -d "$DEV_DIR" "$@"
